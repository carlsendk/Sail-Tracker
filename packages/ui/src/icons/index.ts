/**
 * @file Icon barrel for `@sail-tracker/ui`.
 *
 * Inline `<svg>` JSX is forbidden outside this directory by the
 * `no-restricted-syntax` rule in the root `eslint.config.mjs`. New icons
 * belong here as named exports and should be imported as components.
 *
 * This file is intentionally a placeholder until the first icon ships.
 * The `icons` constant exists so downstream code can stably import the
 * barrel; new icon components should be added as named exports above it.
 */
export const icons = Object.freeze({});
